using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceStationUIControler : MonoBehaviour
{
    public int starBaseId;
    //todo add in references to all the starbase screens

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame

        //todo add in functions to initalize all screens based on the current starbase
    void Update()
    {
        
    }
}
